import React, {useEffect} from 'react'

function ProductView({match}) {
    console.log(match)
    return (
        <div>
            these are the details of your chosen product
        </div>
    )
}

export default ProductView
